<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose dibuja toda la UI; este tema solo configura la ventana. -->
    <style name="Theme.Fichaje" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@color/graphite</item>
        <item name="android:statusBarColor">@color/graphite</item>
    </style>
</resources>
